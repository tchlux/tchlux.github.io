from tlux.plot import Plot


# Make a graph that shows the model architecture.
p = Plot()

# Aggregate inputs (ax and axi).
# Aggregate model (states, dimension).
# Aggregate outputs (vector).
# Mean aggregation, concatenated to fixed inputs (x and xi).
# Fixed model (states, dimension).
# Fixed output (vector).

p.graph()
